"use client";
import { FormEvent, useState } from "react";
import { AdminConfiguration } from "@/components/forms/FormsPortal";
export default function AdminPage(){const [ok,setOk]=useState(false),[email,setEmail]=useState("admin@primephilippines.com"),[password,setPassword]=useState(""),[error,setError]=useState("");const login=async(e:FormEvent)=>{e.preventDefault();const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(!r.ok){setError('Invalid admin email or password.');return}setOk(true)};if(!ok)return <main style={{height:"100dvh",overflowY:"auto"}} className="bg-[#f5f2ed] p-8"><form onSubmit={login} className="mx-auto mt-12 max-w-md bg-white border border-slate-200 p-8 shadow-sm"><h1 className="text-2xl font-serif text-[#003366]">Admin login</h1><input className="mt-6 w-full border p-3" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" className="mt-3 w-full border p-3" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="mt-3 text-red-600">{error}</p>}<button className="mt-5 w-full bg-[#003366] p-3 font-bold text-white">Sign in</button></form></main>;return <main style={{height:"100dvh",overflowY:"scroll",WebkitOverflowScrolling:"touch"}} className="bg-[#f5f2ed] px-4 py-6 md:px-8"><div className="mx-auto max-w-[1600px] pb-24"><AdminConfiguration userEmail={email} username="admin"/></div></main>}
